import React from 'react';

const Task12 = () => {
  const greeting = 'Welcome to JSX';
  const description = 'JSX is a syntax extension for JavaScript that looks like HTML.';

  return (
    <div>
      <h2>{greeting}</h2>
      <p>{description}</p>
    </div>
  );
};

export default Task12;
